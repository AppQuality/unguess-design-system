import { NavItem as ZendeskNavItem } from "@zendeskgarden/react-chrome";
import { NavItemArgs } from "./_types";


const NavItem = (props: NavItemArgs) => <ZendeskNavItem {...props} />;

export { NavItem };
