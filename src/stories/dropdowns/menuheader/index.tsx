import {
  HeaderItem as ZendeskHeaderItem
} from "@zendeskgarden/react-dropdowns";
import { HeaderItemArgs } from "./_types";

export const MenuHeaderItem = (props: HeaderItemArgs) => (
  <ZendeskHeaderItem {...props} />
);
